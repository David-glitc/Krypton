'use client'
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _tslib = require('../../../../../_virtual/_tslib.cjs');
var React = require('react');
var client = require('@dynamic-labs-sdk/client');

const useStepUpMfa = ({ stateActions }) => {
    const { setError, setLoading, setSuccess } = stateActions;
    const verifyPasskeyMfa = React.useCallback((_a) => _tslib.__awaiter(void 0, [_a], void 0, function* ({ requestedScopes, }) {
        setLoading();
        try {
            const response = yield client.authenticatePasskeyMFA({
                requestedScopes,
            });
            setSuccess();
            return response;
        }
        catch (error) {
            const errorMessage = error instanceof Error
                ? error.message
                : 'Failed to authenticate passkey for step-up authentication';
            setError(errorMessage);
            return null;
        }
    }), [setError, setLoading, setSuccess]);
    const verifyTotpMfa = React.useCallback((_b) => _tslib.__awaiter(void 0, [_b], void 0, function* ({ code, deviceId, requestedScopes, }) {
        setLoading();
        try {
            const response = yield client.authenticateTotpMfaDevice({
                code,
                deviceId,
                requestedScopes,
            });
            setSuccess();
            return response;
        }
        catch (error) {
            const errorMessage = error instanceof Error
                ? error.message
                : 'Failed to authenticate TOTP device for step-up authentication';
            setError(errorMessage);
            return null;
        }
    }), [setError, setLoading, setSuccess]);
    const verifyRecoveryCode = React.useCallback((_c) => _tslib.__awaiter(void 0, [_c], void 0, function* ({ code, requestedScopes, }) {
        setLoading();
        try {
            const response = yield client.authenticateMfaRecoveryCode({
                code,
                requestedScopes,
            });
            setSuccess();
            return response;
        }
        catch (error) {
            const errorMessage = error instanceof Error
                ? error.message
                : 'Failed to authenticate recovery code for step-up authentication';
            setError(errorMessage);
            return null;
        }
    }), [setError, setLoading, setSuccess]);
    return { verifyPasskeyMfa, verifyRecoveryCode, verifyTotpMfa };
};

exports.useStepUpMfa = useStepUpMfa;
