import { SigningAlgorithm } from '@dynamic-labs-wallet/primitives';
/**
 * @deprecated Import from '@dynamic-labs-wallet/primitives' instead.
 *
 * Re-exported here temporarily for backward compatibility with already-published
 * versions of @dynamic-labs-wallet/forward-mpc-shared (<= 0.5.2) and
 * @dynamic-labs-wallet/forward-mpc-client (<= 0.8.x) that still import
 * SigningAlgorithm from '@dynamic-labs-wallet/core'. Remove this re-export
 * once those external packages publish versions importing directly from
 * @dynamic-labs-wallet/primitives.
 */
export { SigningAlgorithm };
export declare const BITCOIN_DERIVATION_PATHS: {
    NATIVE_SEGWIT: number[];
    TAPROOT: number[];
};
/**
 * Midnight derivation paths for different address types
 * Based on BIP-44 with role index at position 3:
 * m/44'/2400'/0'/role/0
 */
export declare const MIDNIGHT_DERIVATION_PATHS: {
    UNSHIELDED: number[];
    SHIELDED: number[];
    DUST: number[];
};
/**
 * Bitcoin address type configurations
 * Maps each address type to its derivation path and signing algorithm
 */
export declare const BITCOIN_ADDRESS_TYPE_CONFIG: Record<string, {
    derivationPath: number[];
    signingAlgorithm: SigningAlgorithm;
}>;
export declare const MPC_CHAIN_CONFIG: Record<string, {
    derivationPath: number[];
    signingAlgorithm: SigningAlgorithm;
}>;
export declare enum ThresholdSignatureScheme {
    TWO_OF_TWO = "TWO_OF_TWO",
    TWO_OF_THREE = "TWO_OF_THREE",
    THREE_OF_FIVE = "THREE_OF_FIVE"
}
export declare const MPC_CONFIG: {
    TWO_OF_TWO: {
        numberOfParties: number;
        threshold: number;
        clientThreshold: number;
        dynamicServerThreshold: number;
    };
    TWO_OF_THREE: {
        numberOfParties: number;
        threshold: number;
        clientThreshold: number;
        dynamicServerThreshold: number;
    };
    THREE_OF_FIVE: {
        numberOfParties: number;
        threshold: number;
        clientThreshold: number;
        dynamicServerThreshold: number;
    };
};
export declare enum CreateRoomPartiesOptions {
    THRESHOLD = "threshold",
    FULL = "full"
}
//# sourceMappingURL=constants.d.ts.map