/**
 * Reads signin_credential_id from the current min JWT claims.
 * Returns undefined if no token, decode fails, or claim is missing.
 */
export declare const getSignInCredentialIdFromMinToken: () => string | undefined;
