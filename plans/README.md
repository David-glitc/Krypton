# Implementation Plans

Generated by the improve skill on 2026-06-19. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Wire up wallet connection | P1 | M | — | TODO |
| 002  | Add error boundaries to all route segments | P1 | S | — | TODO |
| 003  | Fix CSS variable naming inconsistency | P2 | S | — | TODO |
| 004  | Add loading states to all route segments | P2 | S | — | TODO |

## Dependency notes

- All plans are independent and can be executed in parallel.
- Plan 001 (wallet connection) should be executed first since it's the highest-impact change and unblocks real user interaction.

## Findings considered and rejected

- **CI workflow references wrong package names**: Rejected — the CI workflow at `.github/workflows/ci.yml` is correct. `pnpm --filter @krypton/ui typecheck` works because `packages/ui/package.json` has a `typecheck` script. The `pnpm build` at line 22 builds the root Next.js app.
- **No test coverage for Next.js app**: Not worth planning yet — the app is mostly UI components with mock data. Test coverage should be added after real data fetching is implemented (future direction finding).
- **Contract not deployed**: Not a code issue — requires devnet deployment which is an ops task, not a code improvement.
- **Orchestrator is a stub**: Not worth planning — the agent pipeline is a Phase 2 feature that requires LLM integration. The types and events are correctly defined for future implementation.
