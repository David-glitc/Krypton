{
  "name": "@krypton/orchestrator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Phase 2: orchestrator service' && exit 0",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.9"
  }
}
